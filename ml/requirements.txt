# Core ML - installed now so Phase 2 (baselines) and Phase 3 (SBERT fine-tuning)
# can reuse the same environment and data loaders built in Phase 1.
torch>=2.1.0
sentence-transformers>=2.5.0

# Data handling
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0 # required by pandas to read .xlsx datasets

# Baselines (Phase 2) - TF-IDF + cosine similarity, and ranking metrics
scikit-learn>=1.3.0

# Fine-tuning (Phase 3) - sentence-transformers' SentenceTransformerTrainer needs HF `datasets` + `accelerate`
datasets>=2.14.0
accelerate>=1.1.0

# Config / utils
PyYAML>=6.0
tqdm>=4.66.0
